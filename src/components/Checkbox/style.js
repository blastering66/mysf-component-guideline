import { StyleSheet } from 'react-native';
import { GRAY_DIVIDER } from '../../utils/ComponentColors';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  label: {
    paddingLeft: 10,
    textAlign: 'left',
  },
});
