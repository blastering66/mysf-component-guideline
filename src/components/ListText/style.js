import { StyleSheet } from 'react-native';
import {
  BLACK,
} from '../../utils/BaseColors';

export default StyleSheet.create({
  wrapperTag: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  activeList: {
    color: BLACK,
  },
});
